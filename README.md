# PPTXtoDOCX_Converter
An application which takes an inputted power point (.pptx) file and converts it to a document (.docx) file, moving all the text and images from the power point into the document.
